/// Earn staking domain layer (ADR-011).
///
/// Entity/repository contracts currently live in `earn_core`; this library
/// marks the required three-layer module layout for the staking sibling.
library;
